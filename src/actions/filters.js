const launchSuccess = (value) => ({
  type: "LAUNCH_SUCCESS",
  value
});

const landingSuccess = (value) => ({
  type: "LANDING_SUCCESS",
  value
});

const yearFilter = (value) => ({
  type: "LANDING_SUCCESS",
  value
});

export default {
  launchSuccess,
  landingSuccess,
  yearFilter
}